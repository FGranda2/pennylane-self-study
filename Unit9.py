from numpy import linalg as LA
import numpy as np
import cmath
import Func_Unit1 as funcs

state_plus = np.array([1/np.sqrt(2),0])
state_1 = np.array([0,1/np.sqrt(2)])

# norm = funcs.inner_product(state_0,state_0)

